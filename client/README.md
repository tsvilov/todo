# Client frontend (placeholder)

This folder contains a minimal React frontend structure (JSX) as a starting template. It includes an auth context, basic pages, and small components.

You can integrate this into your Vite or Create React App setup. Rename or move files into your existing `frontend` project if desired.
