# SaaS Todo (Template)

Template repository scaffolding a TypeScript React frontend (Vite) and a TypeScript Node backend (Express) with Supabase integration.

Follow the per-folder README files to run frontend and backend locally.
